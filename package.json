{
  "name": "snippets",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "wrangler": "^3.86.1"
  }
}
